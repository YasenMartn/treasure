import React from 'react'

const Circle = ({bgColor, left, top, right, bottom}) => {
  return (
    <div className='circle' style={{backgroundColor: bgColor, left: left, top: top, right: right, bottom: bottom}}>
        
    </div>
  )
}

export default Circle